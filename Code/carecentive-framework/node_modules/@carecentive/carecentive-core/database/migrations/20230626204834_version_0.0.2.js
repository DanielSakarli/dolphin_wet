// 06.06.2023: Version 0.0.1
// these tables contain following sections: Good Health
// good health table might still be updated by the vet of the Tiergarten
// still missing and waiting for responsefrom the Tiergarten: Emotional State
exports.up = function(knex) {
    return Promise.all([
        knex.schema.createTable('good_health', function (table) {
            table.increments('health_record_id').primary();
            table.integer('user_id');
            table.integer('dolphin_id');

            table.integer('normal_floatability'); //1, 3
            table.integer('eye_lesions'); //1, 3
            table.integer('visual_cues'); //1, 3
            table.integer('mouth_exam'); //1, 3
            table.integer('respiratory_disease'); //1, 3
            table.integer('force_expiration'); //1, 3
            table.integer('external_disease_signs'); //1, 3

            table.timestamps(false, true); //Timestamp: created at, updated at
        })
    ])
};

exports.down = function (knex) {
    return Promise.all([
        knex.schema.dropTable('good_health')
    ])
};
